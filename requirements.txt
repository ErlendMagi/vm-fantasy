# Cloud (Streamlit Community Cloud) dependencies only.
# Scraper-only deps live in scraper/requirements-local.txt.
# pandas/numpy pinned below 3.x: the model is tested on the pandas 2.x API.
streamlit>=1.49,<2
streamlit-autorefresh>=1.0
pandas>=2.2,<3
numpy>=1.26,<3
plotly>=5.20,<7
requests>=2.31
